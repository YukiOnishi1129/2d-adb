# UI/UXレイアウト設計書

## デザインコンセプト

「エロ・同人サイト＝怪しい」という先入観を覆す、**「二次元の資料館」**を目指す。

### デザイン戦略

**「18禁を扱っているが、見た目は最新のSaaS（ITツール）のように美しく、爆速で、使いやすい」**

### カラーパレット

| 項目 | 色 | 理由 |
|------|-----|------|
| **ベースカラー** | オフホワイト / ライトグレー | 信頼感と「ツール感」、長時間でも目が疲れにくい |
| **アクセント** | ディープネイビー / 落ち着いたパープル | 二次元の幻想的な雰囲気と知的なDB印象を両立 |
| **文字色** | 濃いチャコールグレー | 真っ黒を避けモダンで高級感のある可読性を確保 |
| **余白** | 広めにとる | 競合の「情報詰め込みすぎ」を突く |

### テーマ

- **デフォルト**: ライトモード
- **ダークモード**: `.dark`クラス or `?theme=dark`クエリパラメータ
- X経由の流入は自動でダークモード表示

---

## 共通レイアウト

### ヘッダー（`header.tsx`）

```
┌─────────────────────────────────────────────────────┐
│ 2D-ADB                    作品 サークル 声優 ... [検索] │
│ 声優×性癖で探す検索ツール                               │
└─────────────────────────────────────────────────────┘
```

| 要素 | 仕様 |
|-----|------|
| 位置 | `sticky top-0 z-50` |
| 高さ | `h-16` |
| 背景 | `bg-background/95 backdrop-blur` |
| ロゴ | 「2D」アクセント色 + 「-ADB」前景色 |
| タグライン | `声優×性癖で探す検索ツール` |
| ナビ | 作品一覧、サークル、声優、タグ、おすすめ、セール中（アクセント色） |
| 検索 | `SearchBox` コンポーネント（Fuse.js） |

### フッター（`footer.tsx`）

- サイトリンク、RSS、プライバシーポリシー
- コピーライト

### モバイルナビ（`mobile-nav.tsx`）

```
┌─────────────────────────────────────────────────────┐
│   🏠      🔍       💰       📦       🎙️            │
│  ホーム   検索    セール    作品     声優           │
└─────────────────────────────────────────────────────┘
```

| 要素 | 仕様 |
|-----|------|
| 表示 | 768px未満のみ |
| 位置 | `fixed bottom-0 z-50` |
| 高さ | `h-14` |
| 項目 | ホーム、検索、セール、作品、声優 |

---

## z-indexレイヤー階層

```
z-50: ヘッダー (sticky top-0)
z-50: モバイルナビ (fixed bottom-0)
z-40: 固定購入CTA (fixed bottom-14) ← モバイルナビの上に配置
z-40: セール中固定バナー (sticky top-16) ← 作品詳細ページ
```

**重要**: すべてのページ下部には `pb-32` のパディングを設定し、固定要素でコンテンツが隠れないようにする。

---

## トップページ (`/`)

### セクション構成（上から順）

| 順序 | セクション | コンポーネント | 説明 |
|------|-----------|---------------|------|
| 1 | セールバナー | `HeroSaleBanner` | セール作品を横スクロール表示 |
| 2 | 特集バナー | `FeaturedBanners` | セール特集 + 今日のおすすめ（2カラム） |
| 3 | トレンドチップ | `TrendingChips` | 注目声優・タグのコンパクト表示 |
| 4 | ボイス・ASMRランキング | `HorizontalScrollSection` | 横スクロール＋金銀銅バッジ |
| 5 | ゲームランキング | `HorizontalScrollSection` | 横スクロール＋金銀銅バッジ |
| 6 | 高評価（4.5以上） | `HorizontalScrollSection` | 横スクロール |
| 7 | DLsite人気ランキング | `HorizontalScrollSection` | 横スクロール＋紫バッジ |
| 8 | FANZAランキング | `HorizontalScrollSection` | 横スクロール＋オレンジバッジ |
| 9 | 爆安コーナー | `HorizontalScrollSection` | 500円以下 |
| 10 | セール中 | `HorizontalScrollSection` | 割引率順 |
| 11 | 新着作品 | `HorizontalScrollSection` | 発売日順 |
| 12 | 人気声優 | Badgeリスト | 出演作品数の多い声優 |
| 13 | 人気タグ | Badgeリスト | 作品数の多いタグ |

### HorizontalScrollSection

横スクロール可能な作品カード列。

```
┌─ ボイス・ASMRランキング ──────────── もっと見る →┐
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐           │
│ │ 1  │ │ 2  │ │ 3  │ │ 4  │ │ 5  │ ...       │
│ │Card│ │Card│ │Card│ │Card│ │Card│ →scroll   │
│ └────┘ └────┘ └────┘ └────┘ └────┘           │
└─────────────────────────────────────────────┘
```

| オプション | 説明 |
|-----------|------|
| `showRankBadge` | ランキングバッジ表示（1位金、2位銀、3位銅） |
| `rankBadgeColor` | バッジ色（emerald / purple / orange） |

---

## 作品詳細ページ (`/works/[id]`)

### IDフォーマット

- **数値ID**: `/works/101` - DBプライマリキー
- **RJコード**: `/works/RJ01525290` - DLsite作品コード

両方に対応。SNSシェアからのアクセスも可能。

### レイアウト（上から順）

```
┌─────────────────────────────────────────────────────┐
│ [FeaturedBanners: セール特集 | 今日のおすすめ]       │
├─────────────────────────────────────────────────────┤
│ トップ > 作品一覧 > タイトル                         │  ← パンくず
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │                                                 │ │
│ │              ヒーロー画像                        │ │  ← サムネイル
│ │          [30%OFF]               [ASMR]         │ │  ← バッジ
│ │                                                 │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ [ASMR] ★★★★★ 4.85 (1,234件)                        │  ← カテゴリ+評価
│                                                     │
│ タイトルタイトルタイトル                             │  ← h1
│                                                     │
│ サークル名（リンク）                                 │
│                                                     │
│ 出演: [声優A] [声優B]                               │  ← CVバッジ
│                                                     │
│ [耳舐め] [ヤンデレ] [催眠]                          │  ← タグバッジ
├─────────────────────────────────────────────────────┤
│ [サンプル画像ギャラリー - 横スクロール]              │  ← SampleImageGallery
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ おすすめポイント                                │ │  ← AIレビュー
│ │ この作品は〜〜〜が魅力です                       │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 要約                                           │ │
│ │ 〜〜な内容の作品です                            │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 🎯 こんな人におすすめ                          │ │  ← 緑背景
│ │ 〜〜な人に刺さります                            │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ✨ 刺さりポイント                               │ │  ← 黄背景
│ │ 〜〜がすごい                                    │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ⚠️ 注意点                                      │ │  ← 赤背景
│ │ 〜〜な人は注意                                  │ │
│ └─────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📝 2d-adb編集部レビュー                        │ │  ← 紫グラデ
│ │ 詳細なレビュー...                              │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ [スペック表 - SpecTable]                           │  ← キラーワード
├─────────────────────────────────────────────────────┤
│ ⭐ ユーザー評価                                    │
│ DLsite: ★ 4.8 (500件)  FANZA: ★ 4.7 (300件)       │
├─────────────────────────────────────────────────────┤
│ [セール終了カウントダウン - SaleTimer]             │  ← セール時のみ
├─────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────┐ │
│ │ プラットフォーム │   価格    │ 無料版あり      │ │  ← 価格比較表
│ │──────────────────┼───────────┼────────────────│ │
│ │ DLsite [最安]    │ ¥1,320    │ [🎧無料で試聴] │ │
│ │ FANZA           │ ¥1,540    │ [🎧無料で試聴] │ │
│ └─────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────┤
│ 発売日: 2024-01-15                                 │
├─────────────────────────────────────────────────────┤
│ こちらもおすすめ                                    │
│ ┌────┐ ┌────┐ ┌────┐ ┌────┐                       │
│ │Card│ │Card│ │Card│ │Card│  ← 関連作品4件        │
│ └────┘ └────┘ └────┘ └────┘   (CV2件+タグ等2件)    │
└─────────────────────────────────────────────────────┘
```

### 関連作品（こちらもおすすめ）のロジック

1. **上2件**: 同じ声優の作品（発売日順）
2. **下2件**: タグ/サークル/カテゴリでバリエーション
   - 優先: 同じタグの作品（タグ一致数順）
   - フォールバック: 同じサークル → 同じカテゴリ

### スマホ固定要素

| 要素 | 表示条件 | 位置 |
|-----|---------|------|
| セール中固定バナー | セール中 & スマホ | `sticky top-16` |
| 固定購入CTA | 常時（スマホのみ） | `fixed bottom-14` |

---

## 作品カード（`WorkCard`）

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │    サムネイル（1.91:1）         │ │  ← 横長アスペクト比
│ │                                 │ │
│ │  [30%OFF]              [ASMR]   │ │  ← セール/カテゴリバッジ
│ │                         [92分]  │ │  ← スペックバッジ
│ └─────────────────────────────────┘ │
│                                     │
│ [声優A] [声優B]                     │  ← CVバッジ（最大2件）
│                                     │
│ タイトルタイトル...                 │  ← 2行まで
│                                     │
│ サークル名                          │
│                                     │
│ [耳舐め] [ヤンデレ]                 │  ← タグバッジ（最大2件）
│                                     │
│ ¥1,320 → ¥990 (11円/分)  残り2日   │  ← 価格・単価・残り時間
│                                     │
│ ★★★★★ 4.8 (1,234)                 │  ← 評価
└─────────────────────────────────────┘
```

### スペックバッジ

| カテゴリ | 表示内容 |
|---------|---------|
| ASMR/音声作品 | `[92分]` 収録時間 |
| ゲーム | `[50枚]` CG枚数 or `[5h]` プレイ時間 |

### 単価表示

| カテゴリ | 計算 |
|---------|------|
| ASMR/音声作品 | `(xx円/分)` = 価格 ÷ 収録時間 |
| ゲーム | `(xx円/枚)` = 価格 ÷ CG枚数 |

---

## 一覧ページ共通

### グリッドレイアウト

```css
/* モバイル: 2カラム */
grid-cols-2 gap-3

/* デスクトップ: 4カラム */
md:grid-cols-4 md:gap-4
```

### ページヘッダーカード（`PageHeaderCard`）

各一覧ページ上部に表示される説明カード。

```
┌─────────────────────────────────────────────────────┐
│ 📁 声優名                                          │
│ この声優の作品 XX件                                 │
└─────────────────────────────────────────────────────┘
```

---

## セールページ (`/sale`)

### フィルタ・ソート（`SaleFilterSort`）

```
┌─────────────────────────────────────────────────────┐
│ [全て] [50%OFF以上] [500円以下]                     │  ← フィルタボタン
│                                                     │
│ 並び替え: [割引率順 ▼]                             │  ← ソートドロップダウン
└─────────────────────────────────────────────────────┘
```

### ソートオプション

- 割引率順（高い順）
- 価格順（安い順）
- 新着順
- 評価順

---

## 検索ページ (`/search`)

### 検索システム

- **エンジン**: Fuse.js（クライアントサイド全文検索）
- **インデックス**: ビルド時に `public/data/search-index.json` 生成

### フィルタ

| フィルタ | オプション |
|---------|-----------|
| カテゴリ | 全て / ASMR / ゲーム |
| プラットフォーム | 全て / DLsite / FANZA |
| セール | 全て / セール中のみ |
| 価格帯 | 全て / 500円以下 / 1000円以下 / ... |

### ソート

- ランキング順
- 新着順
- 価格順（安い順/高い順）
- 割引率順
- 評価順

---

## Badgeバリエーション

| variant | 用途 | 外観 |
|---------|------|------|
| `sale` | セール割引率 | 赤背景・白文字 |
| `cv` | 声優名 | アクセント色背景 |
| `tag` | タグ | グレー背景 |
| `secondary` | カテゴリ等 | 薄いグレー背景 |
| `outline` | 最安表示等 | 枠線のみ |

---

## SEO対応

### 構造化データ（JSON-LD）

- **Product**: 作品詳細ページ
- **BreadcrumbList**: パンくずリスト

### メタデータ

- `title`: セール中は `【XX%OFF】` プレフィックス
- `description`: AIの刺さりポイント > おすすめ理由 > 要約
- `og:image`: サムネイル画像
- `keywords`: タグ + CV + サークル + カテゴリ

---

## コンポーネント一覧

| コンポーネント | ファイル | 説明 |
|---------------|---------|------|
| `Header` | `header.tsx` | ヘッダー（ロゴ・ナビ・検索） |
| `Footer` | `footer.tsx` | フッター |
| `MobileNav` | `mobile-nav.tsx` | スマホ固定ナビ |
| `WorkCard` | `work-card.tsx` | 作品カード |
| `HorizontalScrollSection` | `horizontal-scroll-section.tsx` | 横スクロールセクション |
| `FeaturedBanners` | `featured-banners.tsx` | セール特集+おすすめバナー |
| `TrendingChips` | `trending-chips.tsx` | 注目声優/タグチップ |
| `HeroSaleBanner` | `hero-sale-banner.tsx` | トップのセールバナー |
| `SaleTimer` | `sale-timer.tsx` | セール終了カウントダウン |
| `SaleBannerCountdown` | `sale-banner-countdown.tsx` | 固定バナー用カウントダウン |
| `SpecTable` | `spec-table.tsx` | スペック表（キラーワード） |
| `SampleImageGallery` | `sample-image-gallery.tsx` | サンプル画像ギャラリー |
| `ImageGalleryModal` | `image-gallery-modal.tsx` | 画像モーダル |
| `FixedPurchaseCta` | `fixed-purchase-cta.tsx` | スマホ固定購入ボタン |
| `SearchBox` | `search-box.tsx` | ヘッダー検索ボックス |
| `SearchForm` | `search-form.tsx` | 検索フォーム |
| `SearchContent` | `search-content.tsx` | 検索結果表示 |
| `SaleFilterSort` | `sale-filter-sort.tsx` | セールフィルタ・ソート |
| `Breadcrumb` | `breadcrumb.tsx` | パンくずリスト |
| `PageHeaderCard` | `page-header-card.tsx` | ページヘッダーカード |
| `SectionHeader` | `section-header.tsx` | セクションヘッダー |
| `ThemeQueryProvider` | `theme-query-provider.tsx` | `?theme=dark`対応 |
| `ForceDarkMode` | `force-dark-mode.tsx` | 強制ダークモード |
| `AgeGate` | `age-gate.tsx` | 年齢確認モーダル |
| `ProductJsonLd` | `json-ld.tsx` | Product構造化データ |
| `BreadcrumbJsonLd` | `json-ld.tsx` | パンくず構造化データ |

### UIコンポーネント（shadcn/ui）

| コンポーネント | ファイル |
|---------------|---------|
| `Button` | `ui/button.tsx` |
| `Card` | `ui/card.tsx` |
| `Badge` | `ui/badge.tsx` |
| `Input` | `ui/input.tsx` |
